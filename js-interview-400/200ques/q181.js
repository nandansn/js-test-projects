// How do you detect a browser language preference
console.log(navigator.language);

// How do you detect a user's location
console.log(navigator.geolocation.getCurrentPosition());

// How do you detect the user's operating system
console.log(navigator.platform);

// How do you detect the user's operating system version
console.log(navigator.appVersion);

// How do you detect the user's browser type
console.log(navigator.userAgent);

// How do you detect the user's browser version
console.log(navigator.appVersion);

// How do you detect the user's screen resolution
console.log(screen.width + "x" + screen.height);

// How do you detect the user's screen color depth
console.log(screen.colorDepth);

// How do you detect the user's screen pixel depth
console.log(screen.pixelDepth);

// How do you detect the user's screen orientation
console.log(screen.orientation.type);

// How do you detect the user's screen orientation
console.log(screen.orientation.angle);

// How do you detect the user's screen orientation
console.log(screen.orientation.lock);